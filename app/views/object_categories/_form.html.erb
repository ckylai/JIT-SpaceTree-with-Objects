<%= form_for(@object_category) do |f| %>
  <% if @object_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@object_category.errors.count, "error") %> prohibited this object_category from being saved:</h2>

      <ul>
      <% @object_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :deleted %><br />
    <%= f.check_box :deleted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
