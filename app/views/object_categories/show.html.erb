<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag "base" %>
    <%= stylesheet_link_tag "Spacetree" %>

    <!--[if IE]>
      <%= javascript_include_tag "excanvas" %>
    <![endif]-->

    <!-- do no need to explicitly include js files because rails automatically includes them if they're in assets/javascripts thanks to application.js -->
    <!-- <%= javascript_include_tag "jit" %> -->
    <!-- <%= javascript_include_tag "example4-3-rest" %> -->
  </head>

  <!-- render the JIT tree of the current object category in its JSON representation (the object category with the ID specified in the URL will be the root node) -->
  <!-- NOTE: when used with DesignPad's FSD, two parameters (node type and its ID) may be used instead, since the tree is rendered in projects but the root node is the base FSD. -->
  <body onload="init( <%= @object_category.to_json %> );">
    <div id="infovis"></div>
    <div id="log"></div>
  </body>
</html>
